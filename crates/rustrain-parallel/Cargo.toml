[package]
name = "rustrain-parallel"
description = "Process groups, rank layout, parallel sharding specs, collective inference"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

# Invariant I-1: no tch / libtorch / cuda in the dependency closure.
[dependencies]
serde.workspace = true
thiserror.workspace = true
tracing.workspace = true

# For pinning the serialization forms in tests.
[dev-dependencies]
serde_json.workspace = true
